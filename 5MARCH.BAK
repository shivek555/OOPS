#include<stdio.h>
#include <string.h>
void main(){
 char str[]="shivek S MITTAL";
 int len = strlen(str);
 int i,j,x,y;
 for(x=1;i<5;i++){
 for (y=1;j<=i;j++){
  printf("%d",y);
 }
 printf("\n");
 }
    for (int i = 0; i < len; i++) {
        for (int j = 0; j <= i; j++) {
            printf("%c", str[j]);
        }
        printf("\n");
    }
    }
