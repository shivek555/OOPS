#include <stdio.h>
#include <conio.h>
union uj{
char name[30];
float salary;
int work;
} ujob;
struct st{
char name[30];
float salary;
int work;
} sjob;
void main(){
printf("size of union= %d bytes", sizeof(ujob);
}